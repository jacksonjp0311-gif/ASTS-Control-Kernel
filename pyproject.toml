[project]
name = "asts-control-kernel"
version = "1.0.0"
description = "ASTS Control Kernel — bounded corrective control for telemetry-driven stability"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.10"
dependencies = []
