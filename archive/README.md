# Archive

These trees are **not on the runtime path**. They are kept for history.

| Path | Why it is here |
| --- | --- |
| `backup_pre_tune/` | pre-tune recovery copies |
| `structural_snapshot/` | one-off tree dump |
| `experiments/` | empty notebook/scenario stubs |
| `memory/` | episodic/summary placeholders |
| `partition/` | partition/compression placeholders |

Do not import these modules from `engine/`, `runtime/`, or `ledger/`.
If a design returns, promote it back to the root with tests.
